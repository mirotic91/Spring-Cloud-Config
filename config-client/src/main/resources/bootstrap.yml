#spring:
#  profiles:
#    active: real
#
#  cloud:
#    config:
#      uri: http://localhost:8888
#
#  application:
#    name: msa

spring:
  profiles:
    active: dev
  application:
    name: msa

---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8888
---
spring:
  profiles: st
  cloud:
    config:
      uri: http://localhost:8888